table.table.students
  thead
    tr
      th Project
      th Status
      th Marker
      th Stats
      th

  tbody
    /Future to-do: move this logic/database querying into the model
    - @projects.each do |project|
      - evaluations = @student.evaluations_for(project)
      - if evaluations.count > 1
        - evaluations.each do |evaluation|
          = render 'project_evaluation_row', project: project, evaluation: evaluation
      - else
        = render 'project_evaluation_row', project: project, evaluation: evaluations.first
