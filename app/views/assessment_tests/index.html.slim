header.page-header
  h1 Assessment Tests

h2 = cohort.name

.panel.panel-default
  .panel-heading
    h3.panel-title
      | Github usernames

  .table-responsive
    table.table.sortable
      thead
        tr
          th Student
          th GitHub Username
      tbody
        - cohort.students.active.each do |student|
          tr
            td
              .student-info
                == render 'shared/student_info', student: student
            td = student.github_username

h3 Proctologist Dashboards

p = link_to 'Mock Test', url_for_proctologist('web-01', cohort)

p = link_to 'Real Test', url_for_proctologist('web-main', cohort)

p = link_to 'Rest Test (Rollovers)', url_for_proctologist('web-02b', cohort)
