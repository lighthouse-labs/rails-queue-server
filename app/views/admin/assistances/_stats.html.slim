.summary-stats
  .stat.panel.panel-default
    .panel-body
      h3
        i.fa.fa-star
        | &nbsp;
        = @avg_l_score
      span Average L-Score

  .stat.panel.panel-default
    .panel-body
      h3
        i.fa.fa-clock-o
        | &nbsp;
        - if @assist_length_avg.nan?
          = "N/A"
        - else
          = "#{@assist_length_avg.to_i} mins"
      span Average Length

  .stat.panel.panel-default
    .panel-body
      h3
        i.fa.fa-group
        | &nbsp;
        = @distinct_students
      span Students

  .stat.panel.panel-default
    .panel-body
      h3
        i.fa.fa-life-ring
        | &nbsp;
        = @distinct_teachers
      span Mentors
