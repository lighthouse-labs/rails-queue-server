.summary-stats
  .stat.card
    .card-body
      h3
        i.fa.fa-star
        | &nbsp;
        = @activities.where.not(average_rating: nil).average(:average_rating).to_f.round(1)
      span Average Rating

  .stat.card
    .card-body
      h3
        i.fa.fa-folder-open
        | &nbsp;
        = @activities.core.total_count
      span Core Activities

  .stat.card
    .card-body
      h3
        i.fa.fa-fire
        | &nbsp;
        = @activities.stretch.total_count
      span Stretch Activities

  .stat.card
    .card-body
      h3
        i.far.fa-clock
        | &nbsp;
        = prep_time_spent @activities.sum(:average_time_spent)
      span Total Time Spent
